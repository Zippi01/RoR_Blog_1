<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://kit.fontawesome.com/ac7c51298a.js" crossorigin="anonymous"></script>


    <nav class="navbar navbar-expand-lg navbar navbar-dark bg-dark">
      <p class="fas fa-dove " style="color: #51cf66;"></p>
      <%= link_to "Blog", home_path, class: "navbar-brand "%>


      <button class="navbar-toggler" type="button"
              data-toggle="collapse" data-target="#navbarSupportedContent"
              aria-controls="navbarSupportedContent" aria-expanded="false"
              aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <%= form_tag(posts_path, :method => "get", id: "search-form", class:"form-inline center col-xs-4 col-sm-4 col-md-4") do %>
        <%= text_field_tag :search, params[:search], placeholder: "Search Posts", class: "form-control mr-sm-2" %>
        <%= submit_tag "Search", class: "btn btn-outline-success" %>
      <% end %>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
          </li>
        </ul>
        <form class="form-inline my-2 my-lg-0  ">
          <div class="nav-item">
          <% if current_author %>
          <div class="text-white">
            Logged in as <%= current_author.email %>
            <%= link_to "Logout", "/logout", class:'btn btn-outline-success my-2 my-sm-0'  %>
            <%= link_to 'New Post', new_post_path, class:'btn btn-outline-success'%>
              <% else %>
            <%= link_to "Log in", login_path, class:'btn btn-outline-success my-2 my-sm-0'  %>
            <%= link_to "Sign up", signup_path, class:'btn btn-outline-success my-2 my-sm-0'%>
          </div>
          <% end %>
        </div>
        </form>
      </div>
    </nav>
  </head>
  <body>
    <div class="container">
      <%= render partial: 'posts/flash',  flash: flash %>
      <%= yield %>
    </div>
  </body>
  <footer class=" page-footer font-small bg-dark">
    <div class="footer-copyright text-center py-3 text-white">© 2019 RoR Homework 6:
      <strong>RoR_Blog</strong><br>
      <i class="fab fa-facebook"></i>
      <i class="fab fa-github"></i>
      <i class="fab fa-telegram"></i>
    </div>
  </footer>
</html>
